/**
 * @file InvestigationPage.jsx
 * @description Main investigation page — placeholder until AppShell is built in STEP 10.
 */

export default function InvestigationPage() {
  return (
    <div className="flex h-screen items-center justify-center bg-zinc-950 text-zinc-100">
      <p className="font-mono text-amber-400 tracking-widest">FIND PODO — Investigation Board Loading…</p>
    </div>
  );
}
